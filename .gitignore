# full half-edge states (~28 MB each); certificates in certificates/ are the checkable form
*.raw

# python
__pycache__/
*.pyc

# compiled search binary
search/relaxed_walk5
*.o

# search / checker outputs
*-last.json
*-venn.json
*-best.raw
*.err
*.out
monotone-results.json
out/
results/

# latex build products
*.aux
*.log
*.out
*.synctex.gz
*.fls
*.fdb_latexmk

.DS_Store

# Lean build artefacts: .lake/ holds Mathlib and ~8 GB of compiled output
verify/lean/.lake/
verify/lean/build/
# but keep the build transcript that ships with the proof bundle
!verify/lean/verification/full-build.log

# secrets
.env
*.env

# Lean 19 port: build products are not tracked either
verify/lean19/.lake/
verify/lean19/build/
